Задание 1)В векторе нет слова 'да' потому что это так называемое стоп-слово функции to_tsvector.Подобные слова игнорируются функцией,
посколько фигурируют в тексте настолько часто, что искать их бессмысленно.


Задание 2.1)@@ сопоставляет результаты функций tsvector и tsquery.
Задание 2.2)Потому что лексемой слова 'foxhound' явлеяется 'foxhound', а не 'fox'.Поэтому данное слово не нашлось в результате поиска.
Задание 2.3)Потому что у слов 'булка' и 'булок' разные лексемы.
Задание 2.4)Потому что у слов 'пирожок' и 'пирожков' разные лексемы.



Задание 3.1)
SELECT to_tsvector('Russian','Проведение Единого госэкзамена (ЕГЭ) в России не будет отменено даже на фоне распространения коронавируса')  
     @@ to_tsquery('Россия & ЕГЭ');
SELECT to_tsvector('Russian','Проведение Единого госэкзамена (ЕГЭ) в России не будет отменено даже на фоне распространения коронавируса')  
     @@ to_tsquery('Россия | США');
SELECT to_tsvector('Russian','Проведение Единого госэкзамена (ЕГЭ) в России не будет отменено даже на фоне распространения коронавируса')  
     @@ to_tsquery('!США');
SELECT to_tsvector('Russian','Проведение Единого госэкзамена (ЕГЭ) в России не будет отменено даже на фоне распространения коронавируса')  
     @@ to_tsquery('ЕГЭ & (Россия | США)');
Задание 3.2)Потому что Английский язык стоит по умолчанию.



Задание 4.1)То что слово 'мягких' предшествует слову 'булок'.Слово 'булок' находится на 2 позиции от слова 'мягких'
Задание 4.2)
SELECT to_tsvector('Russian', 'Съешь ещё этих мягких французских булок, да выпей чаю.')
    @@ to_tsquery('Russian','съешь<1>ещё');
Задание 4.3)phraseto_tsquery преобразует неформатированный текст запроса в значение tsquery. Текст разбирается и нормализуется подобно тому,
 как это делает to_tsvector, а затем между оставшимися словами вставляются логические операторы <-> (ПРЕДШЕСТВУЕТ).



Задание 5.1)
SELECT ts_debug('Russian','Проведение Единого госэкзамена (ЕГЭ) в России не будет отменено даже на фоне распространения коронавируса');
SELECT ts_headline('Russian','Проведение Единого госэкзамена (ЕГЭ) в России не будет отменено даже на фоне распространения коронавируса',
to_tsquery('ЕГЭ & России'));